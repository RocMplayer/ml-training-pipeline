model:
  type: mlp
  hidden_sizes: [128, 64]
  dropout: 0.1

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  optimizer: adam
  weight_decay: 0.0001

data:
  train_path: data/train.csv
  val_path: data/val.csv

output:
  dir: results
  save_checkpoint: true
  save_report: true
